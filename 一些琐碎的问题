**# 关于stata变量名、变量标签和值标签的相关问题
* 概念辨析
  简单而言，“变量名”就是我们赋予某一组数据的代号
  比如“gender” “age”；“变量标签”就是对这些代号的进一步阐释，如“educ”的变量标签可以是“被访者最高受教育年限”；
  而值标签则是对变量取值的注释，如在“hukou”中，1代表农业户口，0代表非农户口。
* stata值标签赋值逻辑
  在stata中，值标签是你首先定义好了以后，被存到一个值标签组里面，再和变量绑定在一起，才能呈现出来的
      第一步：label define valuelabel num1 "XXX" num2 "XXY" num3 "XYY" //valuelabel 你可以任意取名，它只是代表之后的数值与内涵一一对应都会被放在这个值标签名称之下
      第二部：label values variablename valuelabel //把这个值标签的内容对应到前面那个变量之下
  便捷之处：比如说我们定义了yesno这个值标签组的内容为，1“是” 0“否”，那么在赋予其它二元取值的变量取值含义的时候，直接逐个捆绑即可。
